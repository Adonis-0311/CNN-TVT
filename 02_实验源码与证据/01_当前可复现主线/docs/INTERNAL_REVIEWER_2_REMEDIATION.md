# Reviewer #2 manuscript-only remediation

Date: 2026-07-28

Scope: manuscript, bibliography, and deterministic method illustration only.
No training, evaluation, or experiment-code change is included.

## Closed in the internal-review manuscript

- M01: the exact discrete SNR/SIR grids now match the cache policy.
- M02: inference is explicitly single-view; the paired view is training and
  source-aligned statistical-comparison infrastructure only.
- M03: unanchored in-taxonomy modulated cochannel collisions are excluded, and
  Fig. 2 now uses an admitted structured sweep jammer from the schema-v2 cache.
- M04: route terms are consistently target-power-dominant,
  jammer-power-dominant, and unexplained-or-power-ambiguous.
- M05: typed sentinel values were replaced by fail-closed manifest-bound audit
  gates.
- M08: the primary reference is predeclared before training/test evaluation.
- M11: the hierarchical paired bootstrap is primary; per-seed McNemar is
  supplemental, with the Holm family and its limits stated.
- M12--M13: title, abstract, protocol, limitations, and conclusion are narrowed
  to vehicular-Doppler TDL-profile simulations; no SDR, field, deployment, or
  complete V2X-scenario claim remains.
- O01--O06 and O08--O09: nine protocol splits, single-jammer evidence scope,
  bounded-bypass language and A5--A7 test, composite A6 caveat, third-route
  non-identifiability, oracle spectral component ratio, clean-profile strata,
  and active-jammer orthogonality scope are explicit.
- N05--N06: three unused bibliography records were removed and `cochannel`
  versus `mixed` terminology was defined.
- M09: the runner now serializes the selected epoch, eligibility criterion,
  and fallback state for every fit; the formal release gate requires an
  eligible checkpoint for every model/seed pair and rejects all fallbacks.
- M10: jammer BCE is masked to labels with positive training support,
  unsupported columns receive zero gradient, and held/excluded-family logits
  are explicitly non-interpretable.  The release protocol audits this support
  contract.
- O10: result macros remain fail-closed until the formal cache designation,
  complete model/seed matrix, finite artifacts, unchanged source tree, exact
  provenance, and placeholder-free macro manifest all pass.
- M07 (bounded closure): the 2025 CSSL-AMC official encoder/classifier
  architecture is pinned by commit, file hashes, and license, registered in
  the 1,024-sample formal family, and labeled only as an
  official-architecture supervised adaptation. This closes the narrower need
  for a recent auditable AMC architecture comparator; it is not performance
  evidence and does not close the structured-interference comparison gap.

## Major issues that remain open

1. M06: there is no eligible frozen multi-seed headline result bundle, so the
   result tables correctly remain `pending`.
2. M07 remainder: no verified recent structured-interference-specific
   implementation is executable. MFENet and RepCCNet remain related work, and
   the common-budget literature rows still require a prospectively registered
   architecture-specific tuning-sensitivity analysis before a corresponding
   strongest-method claim.
3. M12: the narrower simulation title removes the overclaim, but a full
   geometry-to-vehicular-scenario mapping would still be required to expand
   beyond TDL-profile simulations.

The dominant submission blocker is M06, followed by the structured-interference
and tuning-sensitivity remainder of M07. None of the open items may be filled
with smoke, screening, or fixed-batch diagnostic performance.
